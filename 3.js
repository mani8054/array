data=[1,2,3,4,5];
    console.log("length"+data.length); //total length print
    console.log("index values "+data[0]); //it will print index value
    data.pop(); //it will delete the last element inside the row
    console.log("after pop:",data);
    data.push(7);//it will push the last element inside the row
    console.log("after push",data);
    delete data[2]; //it will delete the values as per user requirment
    console.log("after delete data values is :"+data);
